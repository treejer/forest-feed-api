export enum CollectionNames {
  USER = "users",
  LAST_STATE = "laststates",
}

export enum Role {
  USER = 1,
  PLANTER = 2,
  ADMIN = 3,
}

export enum UserStatus {
  NOT_VERIFIED = 1,
  PENDING = 2,
  VERIFIED = 3,
}

export enum EventName {}
